#include "camera.h"
#include <linmath.h>

struct Camera {
    vec3 position;
    vec3 rotation;
    vec3 scale;
};

void initCamera(void) {
    // init camera stuff
}

void updateCamera(void) {
    // update camera stuff
}
